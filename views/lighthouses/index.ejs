<%- include('../partials/header') %>
<table id="list">
    <thead>
        <tr>
            <th>Name</th>
            <th>Location</th>
        </tr>
    </thead>
    <tbody>
        <% lighthouses.forEach(function(l) { %>
            <tr>
                <td><%= l.lighthouseName %></td>
                <td><%= l.state %></td>
                <td><a href="/lighthouses/show/<%= l._id %>">DETAILS</a></td>
                <td><a href="/lighthouses/ <%= l._id %>">DELETE</a></td>
            </tr>
            <% }); %>
    </tbody>
</table>



<%- include('../partials/footer') %>

